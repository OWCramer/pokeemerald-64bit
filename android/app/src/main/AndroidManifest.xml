<?xml version="1.0" encoding="utf-8"?>
<!-- versionCode / versionName are set from build.gradle (derived from the git
     tag in CI); do not hardcode them here or the manifest values win. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto">

    <uses-feature android:glEsVersion="0x00020000" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-feature android:name="android.hardware.type.pc" android:required="false" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- allowBackup: the save lives in the app's external files directory
         (Android/data/<pkg>/files, see ResolveSavePath). That is one of the
         directories Auto Backup covers by default, which is what Google One
         lists as this app's data, and at 128 KB it is well under the 25 MB cap.

         It stays covered only while there are no custom backup rules. Declaring
         android:fullBackupContent or android:dataExtractionRules makes the set
         exclusive, so anything added later must include domain="external" or
         the save silently stops being backed up. -->
    <application android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true" >

        <!-- Publishes the save to the Files app without moving it out of
             app-private storage, so it stays inside Auto Backup. The system
             holds MANAGE_DOCUMENTS, so that permission restricts binding to the
             system rather than requesting anything. See
             docs/ANDROID_SAVE_LOCATION.md. -->
        <provider
            android:name="com.owcramer.pokeemerald.SaveDocumentsProvider"
            android:authorities="com.owcramer.pokeemerald.saves"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <!-- SDLActivity's default getLibraries() loads SDL3 then "main" (our
             libmain.so), and getMainFunction() returns "SDL_main". Fully
             qualified because this app's namespace is com.owcramer.pokeemerald,
             not org.libsdl.app. -->
        <activity android:name="org.libsdl.app.SDLActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:preferMinimalPostProcessing="true"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
